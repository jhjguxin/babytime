require "spec_helper"

describe BabyTime::Interface::Content do
  
  it "initializes with a Content" do
    expect{BabyTime::Interface::Content.new(BabyTime::Client.new)}.not_to raise_error
  end
end
