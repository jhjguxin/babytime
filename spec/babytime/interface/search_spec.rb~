require "spec_helper"

describe BabyTime::Interface::Monitor do
  
  it "initializes with a Monitor" do
    expect{BabyTime::Interface::Monitor.new(BabyTime::Client.new)}.not_to raise_error
  end
end
